{
	"$schema": "https://railway.app/railway.schema.json",
	"build": {
		"builder": "NIXPACKS"
	},
	"deploy": {
		"startCommand": "/opt/venv/bin/uvicorn app.main:app --host 0.0.0.0 --port $PORT",
		"healthcheckPath": "/healthz",
		"restartPolicyType": "ON_FAILURE"
	}
}
